@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personlige data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-8">
        <p>Din konto indeholder personlige data, som du har givet os. Her kan du vælge at downloade eller slette disse data.</p>
        <p>
            <strong>
                Bemærk: Hvis du vælger at slette dine data, vil din konto blive slettet permanent og kan <u>ikke</u> gendannes.
            </strong>
        </p>

        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="mb-3">
            <button class="btn btn-primary" type="submit">Download personlige data</button>
        </form>

        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">
                Slet konto og alle data permanent
            </a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}